import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ClockCountdown = () => {
  return (
    <View>
      <Text>ClockCountdown</Text>
    </View>
  );
};

export default ClockCountdown;

const styles = StyleSheet.create({});
